# Bonus Assignment
By Benjamin Medrano
 
Using AutoFixture, and MOQ
# Class
	There is an interface IPerson, used for a person. 
	Each IPerson that is implemented is require to have a name, health amount, and skill amount.

# There is three Unit Tests found in the program. 

-The first test is used to test to randomly generated fixtures and see if they match or not. 

- The second test is used to create a mock person object and compare the generated values with the expected.

- The third test is creating auto data and writing out to the console what that data is. 